import styled from '@emotion/styled';

import Container from './Container';

export default styled(Container)({
  marginTop: '40px',
  marginBottom: '100px',
  paddingLeft: '5px',
  paddingRight: '5px',
});
